{
  "rules": {
    ".read": false,
    ".write": false,
    "households": {
      "$householdKey": {
        ".read": "auth != null && $householdKey.matches(/^[a-f0-9]{32}$/)",
        ".write": "auth != null && $householdKey.matches(/^[a-f0-9]{32}$/)"
      }
    }
  }
}
